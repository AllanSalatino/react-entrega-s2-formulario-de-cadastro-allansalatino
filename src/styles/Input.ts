import styled from "styled-components";

export const Input = styled.input`
  background-color: var(--mediumGrey);
  border-radius: 5px;
  padding: 10px;
  color: var(--white);
  outline: none;
  border: none;
  width: 83%;
  margin: 0px 5% 10px 5%;
`;
